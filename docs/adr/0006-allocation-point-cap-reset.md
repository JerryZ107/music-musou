# ADR 0006 — 点数不用点满，可一键重置

**结论**：每首歌有 **Cap**（总点数上限），多轴共享，**不可能全拉满**。允许**剩点不花**。**Reset** 一键恢复默认或清空。

**为啥**：「不够花」才是取舍；强迫点满反而像作业。

| 谁 | 记什么 |
|----|--------|
| **前端** | 显示「已用 / Cap」；提交时 `sum ≤ cap`；Reset 防误触。 |
| **后端** | 校验 Cap；Reset 写默认表。 |

**别做**：必须点满才能开 Run。

**相关**：0005
